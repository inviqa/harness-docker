        stage('Build') {
            steps {
{% if @('jenkins.tests.use_global_services') %}
                sh 'ws install'
{% else %}
                sh 'ws enable'
{% endif %}
                milestone(20)
            }
        }
